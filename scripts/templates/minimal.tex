% minimal.tex — LaTeX preamble for the minimal export theme
% Clean typography, no color accents. Content-first.
% Used via: pandoc --include-in-header=minimal.tex
% NOTE: fontspec, geometry, hyperref are loaded by Pandoc — do NOT reload them here.

% ── Colors ─────────────────────────────────────────────────────────────────
\definecolor{bodytext}{HTML}{333333}
\definecolor{headingcolor}{HTML}{111111}
\definecolor{rulecolor}{HTML}{CCCCCC}
\definecolor{codebg}{HTML}{F5F5F5}
\definecolor{blockquotebar}{HTML}{CCCCCC}
\definecolor{blockquotetext}{HTML}{555555}

% ── Base Text ──────────────────────────────────────────────────────────────
\color{bodytext}
\usepackage{setspace}
\setstretch{1.4}
\setlength{\parskip}{0.6em}
\setlength{\parindent}{0pt}

% ── Headings ───────────────────────────────────────────────────────────────
\usepackage{titlesec}

\titleformat{\section}
  {\Large\bfseries\color{headingcolor}}
  {}{0em}{}[\vspace{2pt}{\color{rulecolor}\titlerule[0.4pt]}]
\titlespacing*{\section}{0pt}{1.8em}{0.5em}

\titleformat{\subsection}
  {\large\bfseries\color{headingcolor}}
  {}{0em}{}
\titlespacing*{\subsection}{0pt}{1.5em}{0.4em}

\titleformat{\subsubsection}
  {\normalsize\bfseries\color{headingcolor}}
  {}{0em}{}
\titlespacing*{\subsubsection}{0pt}{1.2em}{0.3em}

% ── Code Blocks ────────────────────────────────────────────────────────────
\usepackage{framed}
\definecolor{shadecolor}{HTML}{F5F5F5}

\makeatletter
\@ifundefined{Shaded}{
  \newenvironment{Shaded}{\begin{snugshade}}{\end{snugshade}}
}{
  \renewenvironment{Shaded}{\begin{snugshade}}{\end{snugshade}}
}
\makeatother

% ── Tables ─────────────────────────────────────────────────────────────────
\renewcommand{\arraystretch}{1.4}

% ── Blockquotes ────────────────────────────────────────────────────────────
\usepackage{mdframed}
\renewenvironment{quote}{%
  \begin{mdframed}[
    topline=false, rightline=false, bottomline=false,
    linewidth=2pt, linecolor=blockquotebar,
    innerleftmargin=10pt, innerrightmargin=0pt,
    innertopmargin=4pt, innerbottommargin=4pt,
    skipabove=\parskip, skipbelow=\parskip
  ]
  \color{blockquotetext}
}{%
  \end{mdframed}
}

% ── Prevent Orphans/Widows ─────────────────────────────────────────────────
\widowpenalty=10000
\clubpenalty=10000

% ── Keep floats close to text ──────────────────────────────────────────────
\usepackage{float}
\floatplacement{table}{H}
\floatplacement{figure}{H}
